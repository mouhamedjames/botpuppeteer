<!DOCTYPE html>
<html>
<head>
    <style>
        /* CSS for grid layout and green color scheme */
        body {
            background-color: #e6ffe6;
            font-family: Arial, sans-serif;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin: 20px;
        }

        .item {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.2);
        }

        img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <div class="container">
        <% filter3.forEach((listing) => { %>
            <div class="item">
                <h1><%= listing.title %></h1>
                <img src="<%= listing.images[0] %>" >
                <h2><%= listing.name %></h2>
                <p>Price: <%= listing.facts[0] %></p>
                <p>Bedrooms: <%= listing.facts[1] %></p>
                
            </div>
        <% }); %>
    </div>
</body>
</html>